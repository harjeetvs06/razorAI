services:
  backend:
    build: .
    ports:
      - "8001:8001"
    env_file: .env
    volumes:
      - .:/app

  frontend:
    build: ./frontend
    ports:
      - "8082:8082"
    depends_on:
      - backend
